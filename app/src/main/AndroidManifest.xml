<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.fitsweet">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.FitSweet"
        tools:targetApi="31">

        <!-- Pantalla de Registro -->
        <activity android:name=".RegistroActivity" />

        <!-- Pantalla de Bienvenida -->
        <activity android:name=".BienvenidaActivity" />

        <!-- Pantalla Splash (debe ser la principal al abrir la app) -->
        <activity
            android:name=".SplashActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- MainActivity (opcional, puedes usarla para el Login) -->
        <activity android:name=".MainActivity" />

    </application>

</manifest>
